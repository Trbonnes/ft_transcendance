import { IsChannelMemberGuard } from './is-channel-member.guard';

describe('IsChannelMemberGuard', () => {
  it('should be defined', () => {
    expect(new IsChannelMemberGuard()).toBeDefined();
  });
});
